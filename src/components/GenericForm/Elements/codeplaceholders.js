export default `/**
 * returns {void}
 */
async function execute_me ()
{
      /**
       * * @do - uncomment one of the following lines for basic usacase of business rules or add more code;
       * * @notice - self refer to actual current sector that recieve the api, req is instance of http request;
       * ? @There are prebuild functions can be different base on the customer, contact to your service provider for mode details;
       * * @ help solution-portal.perfectevolution.com/knowledge/index
       * ! always write some description here, to describe what this code is doing so other developers can know how to continue or make changes;
       */
      
      // await self.api_action_createone(gresponse, req); // ! CREATE_ONE
      // await self.api_action_readmany(gresponse, queries, getLimit(), getSkip()*getLimit(), []); // ! READ_MANY
      // await self.api_action_updatebyid(gresponse, req); // ! UPDATE_BYID
      // await self.api_action_readbyid(gresponse, req); // ! READ_BYID
      // await self.api_action_deletebyid(gresponse, req); // ! DELETE_BYID

} 
` 

/**
 * 
 */